<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        	<link rel="stylesheet" href="{asset('jquery-ui.custom/jquery-ui.min.css')">
			<link rel="stylesheet" href="{asset('css/main.css')">
        {% endblock %}
    </head>
    <body>
        {{ render(controller( 'App\\Controller\\MenuController::displayMenu', {slug: 'navbar'})) }}
        {% block body %}
        {% endblock %}
        {{ render(controller( 'App\\Controller\\MenuController::displayMenu', {slug: 'footer'})) }}

        {% block javascripts %}
        {% endblock %}
    </body>
</html>
